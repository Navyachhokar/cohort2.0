// console.log("hello");
//use `node {filename}` to run this file on the terminal outside of the browser.

const catMe = require("cat-me"); // require package

console.log(catMe()); //call it and log it